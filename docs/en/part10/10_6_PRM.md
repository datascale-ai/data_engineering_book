# Project 6: CoT inference data set construction and PRM training

## Overview of this chapter

P06 focuses on organizing the inference process itself into trainable, verifiable, analyzable, and iterable process supervision data assets. The focus of this chapter is not on the display of a single thinking chain, but on the engineering connection between step-level supervision, reward distribution and PRM training interface.

This chapter can be understood according to four main lines:

* Seed tasks and trajectory generation: From task sampling to CoT trajectory construction.
* step verification and reward distribution: organize supervision signals around process labels, reward buckets and trajectory types.
* PRM encapsulation and training segmentation: organize the processing results into process supervision data that can be directly trained.
* Measurements and Project Checks: Validate data factory status through metrics, check scripts, and noise analysis.

If read in engineering order, this chapter corresponds to a complete link:

**Task sampling -> trajectory generation -> step verification -> reward allocation -> PRM packaging -> training segmentation -> data evaluation -> project inspection**

The core goal corresponding to this structure is to expand CoT and PRM data from result supervision to a process supervision pipeline built around step-level signals.

---

## 1. Project background: The necessity of CoT and PRM data factory

General-purpose large models already have strong capabilities in open domain generation tasks, but once they enter mathematical solution, code reasoning, complex planning or multi-step judgment scenarios, problems will be quickly exposed.

The most common problem is not "the model cannot speak", but "although the model can speak, it does not reason stably according to the correct process."

The first type of problem is that the result is correct but the process is unusable. The model may give the correct answer, but the intermediate reasoning is just template layout, posterior explanation or even random splicing. If such a trajectory is directly used for supervision, what the model learns is not the reasoning ability, but a "language packaging technique for the correct answer."

The second type of problem is that the process seems reasonable, but the key steps are wrong. Such samples will only be classified as "wrong answers" for result supervision, but for process supervision, what is really important is the positioning: whether the condition identification is wrong, the formula substitution is wrong, the code execution is wrong, or there is a problem with the repair path itself.

The third type of problem is that errors are not evenly distributed. Positive trajectories tend to be cleaner, while negative and inpainted trajectories are more prone to noise, label ambiguity, and intermediate state inconsistencies. If the project does not manage these trajectories separately, it will lead to signal contamination during PRM training.

Therefore, the goal of P06 is not to make another "SFT small sample with CoT", but to build a **CoT and PRM data factory** to organize seed tasks, trajectory generation, step labels, verification signals, process rewards and training interfaces into a reusable, checkable, and scalable production line.

This production line does not serve a one-time experiment, but a more universal methodology:

> When the team needs to extend process supervision from mathematics and code to tabular reasoning, tool invocation, agent planning and even complex business processes in the future, what can really be reused is not a certain chain of thinking, but this set of engineering methods of "supervision from tasks to steps".

---

## 2. Project goals and boundaries

### 2.1 Project Goals

This project focuses on the following four goals.

**Goal 1: Establish a transformation link from seed tasks to step-level supervision. **
In other words, the project does not stay at the question and answer level, but the multi-step reasoning trajectory is explicitly generated and further divided into step records that can be used for PRM training.

**Goal 2: Establish a process data system that can distinguish different trajectory types. **
The project clearly distinguishes three types of trajectories: positive, negative, and repair, so that process supervision no longer only has two coarse-grained states of "right/wrong", but can express data distribution closer to the real reasoning process.

**Goal 3: Establish a closed loop of automatic verification and reward distribution. **
The difficulty in process supervision is not generation, but screening. Through rule checking, execution verification, result comparison and reward bucket, the project transforms "trajectory quality" from subjective judgment into a reviewable signal as much as possible.

**Goal 4: Output PRM data assets that can be directly consumed on the training side. **
The final delivery is not just the intermediate script, but also includes `prm_step_dataset.jsonl`, `train.jsonl`, `val.jsonl`, `smoke_test.jsonl`, `training_manifest.json` and other training interface layer products.

### 2.2 Project Boundaries

In order to maintain project reproducibility, this project explicitly sets several boundaries.

#### 1) Task scope boundary

The current project only covers two types of reasoning tasks: **math** and **code**. These two areas are suitable for process supervision demonstration first, because they are relatively easy to construct verifiable signals, and it is easier to align wrong steps with the final results.

#### 2) Track type boundary

Current trajectories are divided into three categories:

*positive
*negative
*repair

Such a design is enough to demonstrate the basic form of process supervision, but it has not yet covered more complex trajectory relationships, such as multi-branch search, tool call rollback, planning-execution hybrid links, etc.

#### 3) Supervision of granularity boundaries

This project emphasizes step-level supervision, but mainly focuses on rule verification + heuristic scoring + data encapsulation, rather than fully developing a large-scale PRM model training platform. In other words, it is more like a prototype of a process supervision data factory rather than the final form.

#### 4) Scale Boundary

There are currently 36 seed tasks, 108 generated trajectories, and a total number of steps of 534. The scale is controllable and is more suitable for method demonstration and structural analysis rather than a large-scale industrial training library. The existing data is enough to explain the process, indicators and noise issues, but it cannot be exaggerated to "cover a wide range of inference scenarios".

### 2.3 The role of boundary setting

The most common misjudgment in process supervision projects is to mistakenly think that a mature PRM data system has been developed when there are CoTs, step labels, and rewards. In fact, a trusted data factory is not built by terminology, but by boundary management.

For P06, the value of clearly written boundaries is:

* Keep the task domain on which the current conclusion depends clear;
* Avoid misinterpreting small-scale verification results as general conclusions;
* Help clarify priorities during subsequent expansion instead of blindly expanding;
* Make the project more like a sustainable evolving engineering capability rather than a concept demonstration.

---

## 3. Project positioning: P06’s capability chain position

If the whole book is regarded as a large model data engineering capability chain, then P06 is at the core of the section "Result Supervision to Process Supervision".

Previous chapters have discussed general issues such as SFT data, preference pairs, evaluation and QA. But once entering the reasoning scene, the team will quickly discover:

* Simple result labels cannot express intermediate errors;
* Simple preference comparison is not enough to locate step-level signals;
* Simply "showing the chain of thought" does not mean that process supervision is trainable;
* Data verification before training determines the final upper limit more than the training itself.

Therefore, the value of this chapter is not to repeat the introduction of PRM concepts, but to put these methods back into an executable engineering project: CoT inference data set construction and PRM training preparation.

That is, this chapter is not an answer to “What is PRM?” but to a more specific question:

* How should process monitoring data be organized?
* Why was the step tag designed earlier than PRM training?
* Why can't negative and repair trajectories be mixed into the same type of bad samples?
* Why is the reward bucket not the icing on the cake, but an important bridge for subsequent training interfaces?
* With limited resources, how to first create a PRM data closed loop that can be run, reviewed, and continuously expanded?

In this sense, the most important thing in this chapter is not to "train one more model", but to answer a larger question:

> How should data engineering be redesigned when the team wants the model to learn better processes, rather than just learn better results?

![Figure 1: CoT and PRM data factory overview](../../images/part10/10_6_fig01_prm_factory_overview.png)

---

## 4. Overall architecture: process supervision pipeline from seed tasks to PRM training assets

From an engineering perspective, this project can be broken down into three floors.

### 4.1 The first layer: task and trajectory generation layer

This layer solves "how to stably obtain an analyzable reasoning trajectory". Mainly include:

* Task sampling
* Specification definition
*Multiple trajectory generation
* Track type control
* Ratio of positive and negative repair samples

The goal of this layer is not to output the training set immediately, but to expand the original task into a set of comparable, analyzable, and verifiable reasoning traces.

### 4.2 The second layer: step verification and reward distribution layer

This layer addresses "whether these processes are worth learning." Mainly include:

* step segmentation
* Rule checking
* Result execution and comparison
* step label generation
* reward bucket allocation
* process-only signal extraction

This is the most critical part of the entire project, because it determines whether the system learns whether it can "write a thinking chain" or "accumulate reliable process signals."

### 4.3 The third layer: training encapsulation and delivery layer

This layer addresses "whether these process data can be directly consumed by the training and evaluation system." Mainly include:

* PRM step data encapsulation
* train/val segmentation
* smoke test construction
* manifest generation
*Dataset evaluation
* Project check script

At this point, the project went from "generating some reasoning traces" to "building a process supervision data pipeline."

![Figure 2: Step-level verification and training closed-loop diagram](../../images/part10/10_6_fig02_step_validation_loop.png)

---

## 5. Seed task: the task layer serves as the starting point for supervision

When many people talk about PRM, they will focus all their attention on the reward or scoring model, but ignore the upstream task layer design.

But in real engineering, the upper limit of PRM training depends largely on: **What task, difficulty, and type of process you let the system see first. **

### 5.1 Why seed tasks are necessary

Without a clear task pool, the project can easily degenerate into "randomly generating some reasoning traces", which may seem like a lot, but are actually chaotically distributed and unevenly difficult, and cannot explain why these steps are worthy of supervision.

After making the task layer explicit, the project can:

* Constrain the source and specifications of the questions;
* Maintain a basic balance among different task domains;
* Track which seed a certain track comes from;
* After training, check whether the problem lies in the task, trajectory or verification process.

### 5.2 Why choose mathematics and code first for the current project?

Mathematics and coding are two great starting points in process supervision.

On the one hand, they all have relatively clear correctness standards, which facilitates result comparison and automatic verification; on the other hand, they have strong enough multi-step reasoning properties to truly expose step-level errors, rather than simply classifying right and wrong.

### 5.3 Why the seed task is not equal to the final training sample

The seed task is only a layer of "supervision source", not the final supervision unit. The real value of P06 is that it expands the seed task into multiple trajectories, then cuts the trajectory into steps, and finally exports PRM data. This link means:

> The project focuses not on the topic itself, but on the process signals generated around the topic.

---

## 6. Task sampling and specification design: pre-scheduling layer

`sampler.py` is responsible for constructing two types of seeds from GSM8K and MBPP at this layer: the math task extracts the final answer and splits it into reference steps, the code task retains `reference_code`, `test_setup_code` and `test_list`, and finally falls to `seed_pool.jsonl` and `task_spec.json`. This way the required fields for subsequent trace generation and verification can be prepared at the upstream stage.

The corresponding implementation is as follows:

```python
for index, record in enumerate(gsm8k):
    final_answer = extract_final_answer(record["answer"])
    steps = split_reasoning_steps(record["answer"])
    if not final_answer or len(steps) < 2:
        continue
    seeds.append(
        {
            "seed_id": f"math_{index}",
            "domain": "math",
            "topic": infer_math_topic(record["question"]),
            "question": record["question"],
            "reference_steps": steps,
            "final_answer": final_answer,
            "source_dataset": "gsm8k_train",
        }
    )
```

`task_spec` writes project constraints as structured configuration:

```python
task_spec = {
    "seed_count": len(seeds),
    "domain_distribution": dict(Counter(seed["domain"] for seed in seeds)),
    "trace_targets": {
        "positive": "correct reasoning trace",
        "negative": "corrupted or wrong reasoning trace",
        "repair": "wrong step followed by correction",
    },
    "validation_targets": [
        "final_answer_match",
        "step_level_quality_labels",
        "code_execution_and_unit_tests",
    ],
}
```

The first step in the project process is `src/sampler.py`: first sample tasks and generate specifications, and then enter inference trajectory generation. P06 Treat “data distribution” from the beginning as an engineering object that needs to be managed explicitly, rather than leaving it to model randomness.

### 6.1 What problems does specification design solve?

The so-called task spec is essentially answering several questions:

* Is the current task mathematics or coding?
* What is the approximate difficulty range of the questions?
* Is a specific structure required for trajectory generation?
* Does subsequent verification rely first on rules, execution or result comparison?

By forwarding these issues to the specification layer, subsequent project generation, verification, and analysis will have unified boundaries.

### 6.2 Why sampling can’t just be “diversified”

Many projects say they have done sampling, but in fact they just randomly select questions. But for PRM, random does not equal reasonable. Because what the process supervision is more concerned about is not the surface variety of the questions, but:

* Whether the error types are rich enough;
* Whether the step granularity is convenient for segmentation;
* Verify whether the signal is available;
* Is there a basis for comparison between positive and negative repair trajectories?

### 6.3 Engineering value of task spec

The current project clearly outputs `seed_pool.jsonl` and `task_spec.json`, which shows that it does not hide the task definition in the code, but explicitly puts the upstream scheduling information on the disk. The benefits of doing this are:

* The impact of different specifications on trajectory quality can be compared later;
* You can track whether certain types of tasks are more likely to generate noise during review;
* The task layer and trajectory layer can be iterated separately instead of rewriting the entire chain each time;
* Interfaces can be reserved for future expansion into new task domains.

![Figure 3: Task sampling and specification generation flow chart](../../images/part10/10_6_fig03_task_sampling.png)

---

## 7. Trajectory generation: parallel construction of positive, negative, repair

The three types of trajectories of P06 are explicitly constructed in `generate_traces.py`: positive examples directly use the reference step; negative examples create errors by tampering with key numbers or mutating reference codes; repair trajectories append a correction step after the error trajectory. This implementation implements "process supervision" into actionable data synthesis logic.

The corresponding implementation is as follows:

```python
wrong_steps = [dict(step) for step in correct_steps]
wrong_steps[-1]["text"] = corrupt_numeric_text(wrong_steps[-1]["text"])
wrong_steps[-1]["label"] = 0

negative_final = {
    "step_idx": len(wrong_steps) + 1,
    "text": f"Final answer: {corrupt_numeric_text(seed['final_answer'])}",
    "label": 0,
    "kind": "final",
}

repair_steps = wrong_steps + [negative_final]
repair_steps.append(
    {
        "step_idx": len(repair_steps) + 1,
        "text": f"Correction: the previous arithmetic was wrong. The correct final answer is {seed['final_answer']}.",
        "label": 1,
        "kind": "repair",
    }
)
```

This implementation shows that the repair trace is not a new question constructed separately, but a repair step is explicitly added after the negative trace. The implementation logic of code tasks is similar, except that errors come from `mutate_python_code`, and verification relies on subsequent unit test execution.

One of the core processes of P06 is `src/generate_traces.py`, which generates multiple reasoning trajectories based on seed tasks instead of only retaining one standard answer trajectory.

### 7.1 Why can’t we only generate positive trajectories?

If the process supervision only retains positive trajectories, the model can indeed learn "what the correct-looking process looks like", but it still cannot effectively distinguish:

* Which partial steps are unreliable;
* Which repair methods are effective;
* Which errors will be amplified in subsequent steps.

This can lead to a PRM that can easily degenerate into a scorer that favors lengthy and neatly formatted trajectories rather than a model that can identify process quality.

### 7.2 What problem does the negative trajectory solve?

The value of a negative trajectory is that it provides an explicit frame of reference for the "error process". Through it, the system can learn:

* How intermediate steps deviate from the correct path;
* What are the precursors before final failure;
* Which errors are just local deviations, and which errors will cause the entire link to collapse.

### 7.3 Why repair trajectory is more important and more dangerous

The repair trajectory is closer to the inference revision process in the real world than ordinary negative examples. It helps the system learn:

* How to return to the correct path after an error occurs;
* What types of remedies are effective;
* What context should be preserved during the repair process.

But the repair trajectory is also the most likely to introduce noise. Because if the repair logic itself is unstable, what the model learns may not be "how to correct errors", but "how to repackage errors into seemingly reasonable continued reasoning."

### 7.4 What does the trajectory structure of the current project indicate?

Existing indicators show that the project generated 108 trajectories, of which three types of trajectories are completely symmetrical: `positive=36`, `negative=36`, `repair=36`. This shows that the trajectory structure of the project is not to temporarily generate several samples, but to clearly design the three types of processes as parallel supervision objects.

![Figure 4: Schematic diagram of the relationship between three types of trajectories](../../images/part10/10_6_fig04_trace_types.png)

---

## 8. Step segmentation and schema: the smallest unit of process supervision

After completing the trajectory generation, the project will not directly use the entire reasoning trace for training, but first perform step-level segmentation. This step is one of the most essential differences between P06 and ordinary CoT sample libraries.

### 8.1 Why step is a necessary unit

The entire trajectory can only express "whether it is good or not in general", but cannot express "specifically which step is good and which step is bad".

What PRM wants to learn is precisely this more fine-grained judgment ability:

* Whether a certain step is logically coherent;
* Whether a certain step is consistent with the previous step;
* Whether a certain step introduced wrong facts or wrong codes;
* Whether a certain step has raised a red flag even if it did not directly lead to the final failure.

### 8.2 What problem does schema solve here?

The so-called step schema is not to make the fields more complete, but to allow verification, statistics and training to all work around the same unit. A typical step record should at least contain:

* `trace_id`
* `task_id`
* `domain`
* `trace_type`
* `step_index`
* `step_text`
* `step_label`
* `reward_bucket`
* `validation_flags`
* `final_outcome`
* `metadata`

With this layer of schema, the project can be followed up with:

* Analyze the quality differences of steps in different domains;
* Compare the noise structure of different trace types;
* Track what step distribution a certain reward bucket comes from;
* Directly encapsulate step data into PRM training interface.

### 8.3 Why step schema must be kept separately

Because many process supervision projects fail, it is not because the model is too poor, but because the minimum supervision unit is not clearly designed from the beginning, resulting in:

* During training, only the entire answer can be scored;
* Unable to review specific steps when an error occurs;
* Data segmentation can only be roughly processed at the sample level;
* There is no gripper for noise analysis.

From this perspective, step schema is not ancillary design, but the foundation of the PRM data factory.

![Figure 5: PRM step schema diagram](../../images/part10/10_6_fig05_step_schema.png)

---

## 9. Automatic verification: verification of results of process supervision

`validate_and_score.py` is responsible for stringing together domain routing, result verification, `trace_score` calculation and reward bucket mapping at this layer: final answer matching for math tasks, code execution and unit testing for coding tasks. In this way, automatic verification can be implemented as a complete closed-loop project.

The corresponding implementation is as follows:

```python
if trace["domain"] == "math":
    passed, validation = validate_math_trace(trace)
else:
    passed, validation = validate_code_trace(trace)

label_sum = sum(step["label"] for step in trace["steps"])
score = label_sum / max(1, len(trace["steps"]))
bucket = reward_bucket(score)

enriched["validation_passed"] = passed
enriched["trace_score"] = round(score, 4)
enriched["reward_bucket"] = bucket
```

Among them, the reward bucket is not a black box score, but a segmented function that can be interpreted by rules:

```python
def reward_bucket(score: float) -> str:
    if score >= 0.95:
        return "high"
    if score >= 0.6:
        return "medium"
    if score > 0:
        return "low"
    return "zero"
```

The advantage of writing this way is that the signals that the verification relies on, the source of the bucket, and the relationship between them become clear.

The intermediate core step of P06 is `src/validate_and_score.py`, which is to verify, score and assign labels to the generated trajectories. This step is very critical because it determines whether the project is accumulating supervision signals or amplifying generated noise.

### 9.1 Why verification must be preceded by the data production process

Many teams understand verification as post-training evaluation, but process supervision is not like this. Because once dirty trajectories enter the PRM data, the model trained later is likely to be better at identifying "pseudo-inference patterns" rather than being better at identifying the true process quality.

Therefore, verification here is not the end of the project, but part of the production line.

### 9.2 What signals can automatic verification rely on?

Judging from the existing process description, the project will combine **rule checking, execution and result comparison** to assign labels and rewards to trajectories and steps. This shows that the current verification does not stay on the surface of the text, but tries to combine formal correctness, executability and result consistency.

In mathematical tasks, this verification is more likely to rely on result comparison; in code tasks, it is more suitable to combine execution results, test samples, or syntax/behavior checks. This design makes "step quality" no longer just a subjective description.

### 9.3 Why is verification not as strict as possible?

Intuitively, many people feel that as long as the rules are tightened, cleaner data can be obtained. However, in process supervision projects, too strict verification can also cause problems:

* Valuable intermediate errors were excessively removed;
* repair track loses its meaning;
* The data distribution is dominated by positive examples, and it is difficult for PRM to learn the error correction boundary;
* Project becomes outcome-only variant.

Therefore, a more reasonable strategy is not to "cleanse across the board" but to preserve structured differences: which steps passed, which steps failed, and which steps failed but are still worth retaining as process signals.

### 9.4 What does the verification result of the current project indicate?

Existing indicators show that the overall trajectory verification pass rate is `67.59%`, but the positive trajectory pass rate reaches `100.00%`, indicating that the current problem is mainly focused on the control of negative and repair trajectories. This result is very valuable because it clearly points out that subsequent optimization of the project should not blindly expand the volume, but should prioritize improving the quality of cleaning and verification.

![Figure 6: Step verification and result comparison flow chart](../../images/part10/10_6_fig06_validation_pipeline.png)

---

## 10. Step label design: operationalization of process fields

When many people talk about process supervision, they use some abstract concepts, such as "high-quality thinking chain", "credible steps" and "process consistency". But what can really be put into practice in the project is not these abstract words, but the data labels that can be entered into fields, statistics and training.

### 10.1 The role of step label

The value of step label is that it breaks down "process quality" into machine-consumable supervision signals. Through tags, projects can identify:

* Which steps should be strengthened;
* Which steps should be punished;
* Which steps are process-specific signals rather than appendages to the outcome;
* Which steps are in the negative trajectory, but are still locally valuable.

### 10.2 Why tags cannot only be used for binary classification

Simple positive and negative classification is certainly attractive because it is simple to implement and intuitive to train. But this is often too crude for PRM. The reason is:

* A repair track may have an error in the first half and a correction in the second half;
* A negative trajectory may have only one critical error;
* Some steps, while not optimal, should not be equated to pure noise.

Therefore, P06 introduces the joint structure of step label and reward bucket, which essentially avoids rudely compressing all complexity into one label bit.

### 10.3 Why is process-only supervision signal important?

Existing indicators show that there are `144` process-only supervision signal steps in the project. The engineering implications of this number are clear: process monitoring does provide additional signals that outcome-only alternatives cannot replace. If you only look at the end result, the value of these 144 steps is simply ignored.

This just shows that the PRM data factory is not "just taking apart the answers", but building a new supervisory layer beyond outcome.

![Figure 7: Schematic diagram of step label and process-only signal](../../images/part10/10_6_fig07_step_labels.png)

---

## 11. Reward bucket: hierarchical scoring mechanism

When many teams do process scoring, they tend to simplify the entire problem into "giving a score to the trajectory." Of course, this approach can run through quickly, but it will also quickly encounter problems: the scores are too sparse, the boundaries are too subjective, and the training interface is unstable.

### 11.1 Engineering significance of reward bucket

The value of reward bucket is not that it is more "advanced" than continuous fractions, but that it is more suitable for expressing interpretable intervals in the early engineering stages. With buckets, teams can more clearly differentiate between:

* A high-quality process that clearly deserves reinforcement;
* An intermediate process that has certain value but is not stable enough;
* Low-quality processes that should be downgraded or even eliminated.

### 11.2 Why bucket is more stable than continuous fractions

In small-scale process supervision projects, continuous scores often cause two problems:

* The scoring caliber is difficult to stabilize;
* It is subsequently difficult to explain where the difference between 0.73 and 0.78 comes from.

In contrast, buckets make it easier for a project to:

* The rules are clearly defined;
* Distribution statistics are straightforward;
* Training mapping is simple;
* Easier to locate abnormal samples during review.

### 11.3 What does the bucket structure of the current project indicate?

The existing reward bucket distribution is: `high=36`, `medium=45`, `low=27`. This distribution means at least two things:

First, rather than making all trajectories “averagely good”, the project creates distinguishable hierarchies of quality.
Second, the current data are more biased toward medium- and high-quality processes, but still retain enough low-quality samples to provide a basis for comparative learning for PRM.

---

## 12. PRM data encapsulation: training interface layer

This section emphasizes the actual structure of the step-level dataset. P06 does not copy the entire trace directly to the training directory, but reorganizes each step into a PRM training record, and then generates `train.jsonl`, `val.jsonl`, `smoke_test.jsonl` and `training_manifest.json`. This shows that the smallest unit consumed by the training system is not questions, but step records with labels and rewards.

The corresponding packaging structure is as follows:

```python
record = {
    "record_id": f"{trace_id}_step_{step_idx}",
    "domain": domain,
    "trace_type": trace_type,
    "prompt": question,
    "step_text": step_text,
    "label": step_label,
    "reward_bucket": reward_bucket,
}
```

With this paragraph, the main text will be more like a project implementation rather than a summary of results.

After completing trajectory generation, step segmentation, verification and reward distribution, the project still needs to complete an often overlooked step: repackaging these intermediate signals into a data format that can be directly consumed by the training system.

In the process of P06, this step is responsible for `src/prepare_prm_data.py`. Its significance is that what the project ultimately builds is not a bunch of scattered intermediate files, but a set of truly trainable data assets.

### 12.1 Why the encapsulation layer is important

If there is no encapsulation layer, the project will have a common fault:

* The research side says "We already have step labels";
* The training side says "I cannot use these files directly";
* The reviewer said "I don't know how train/val is cut".

The role of the encapsulation layer is to reorganize the previous complex process supervision signals into standard assets that can enter the training and evaluation system.

### 12.2 What key training products are output by the current project?

Existing reports show that the project has clearly output:

* `data/training/prm_step_dataset.jsonl`
* `data/training/train.jsonl`
* `data/training/val.jsonl`
* `data/training/smoke_test.jsonl`
* `data/training/training_manifest.json`

This shows that the project has not stayed at the "analysis trajectory" stage, but has completed the implementation of the training interface layer.

### 12.3 Why smoke test and manifest must also exist

Many data projects will focus on train/val but ignore smoke test and manifest. In fact, these two items are very important.

* `smoke_test.jsonl` indicates that the project considers rapid verification before training;
* `training_manifest.json` indicates that the project considers data version, scale, segmentation and meta-information management.

Such products will not directly improve the model score, but will significantly improve the maintainability and reproducibility of the project.

![Figure 8: PRM data encapsulation and training interface diagram](../../images/part10/10_6_fig08_training_interface.png)

---

## 13. Data scale and structure: shaping signals of the current factory

During the project review, it is easy for everyone to first ask "How many pieces of data have been produced?" But for PRM, of course size matters, but structure matters even more.

A few key figures for existing projects are as follows:

* Seed tasks `36`;
* Track `108` items;
* Total number of steps `534`;
* process-only supervision signal steps `144`;
* There are `534` step-level records in the training set;
* Total token estimate `58381`.

### 13.1 What do these numbers say?

First, the project has formed a complete "task -> trajectory -> steps -> training data" extension link, rather than an isolated collection of CoT samples.

Second, the number of steps is significantly higher than the number of tasks, indicating that the supervision unit has successfully moved to the process level instead of remaining at the problem level.

Third, `144` process-only signal steps indicate that the project truly introduced new signals beyond outcome, rather than just rearranging the answer text.

### 13.2 Why structure is more important than total quantity

If a PRM data set only has many steps, but does not:

* Trajectory type distinction;
* reward layering;
* Verification closed loop;
*Clearly segmented;

Then its value is still limited.

P06 What is most worthy of preservation at present is precisely that these structural designs are already in place. Although the scale is small, the closed loop is relatively complete. The most critical point here is: **Process supervision does not start with volume, but with structure. **

---

## 14. Indicator interpretation: the meaning of the current verification results

In many cases, when writing results, they like to report only a total number, such as "more than 100 trajectories were generated." But what's really more valuable are the metrics that help understand system behavior.

### 14.1 Current key indicators

The most critical outcomes of existing projects include:

* Overall verification pass rate: `67.59%`
* Positive trajectory pass rate: `100.00%`
* The number of three types of trajectories is symmetrical: `36 / 36 / 36`
* reward bucket distribution: `36 / 45 / 27`

Looking at these indicators together, they are far more explanatory than reporting a "number of tracks" alone.

### 14.2 Why a pass rate lower than 100% is a good thing

Many projects will regard “all passes” as success. But for process supervision, if all traces are verified to be of high quality, it is often more worthy of vigilance. Because that could mean:

*Only positive examples are retained;
* The verification rules are too loose;
* Noise is ignored;
* PRM cannot learn true boundaries.

P06 Although the current overall pass rate of `67.59%` is not high, it really exposes the noise challenge of negative and repair trajectories. This engineering state of "exposing problems" is often more valuable than pretending that everything is perfect.

### 14.3 Why does the positive example 100% pass also have a double meaning?

The pass rate of positive traces reaching `100.00%` certainly indicates that the quality of forward sample generation is better, but it also reminds another thing: the main pressure of the current project is no longer on positive traces, but on the cleaning and control of error traces and repair traces.

This shows that the next optimization direction of the project is clear, instead of being in a chaotic state where "there may be problems everywhere".

![Figure 9: Verification pass rate and trajectory type comparison chart](../../images/part10/10_6_fig09_validation_metrics.png)

---

## 15. Evaluation and project inspection: self-inspection layer

`run_p6_checks.py` writes a set of specific project access control as automatic checks: first run `py_compile` and `evaluate_prm.py`, and then check whether the required files exist, whether both math/code domains appear, whether the three types of positive/negative/repair trajectories are covered, whether the step label covers both positive and negative categories, whether the reward bucket is complete, and whether train/val overlaps.

The corresponding implementation is as follows:

```python
dataset_checks = [
    {
        "name": "required_files_exist",
        "passed": all(path.exists() for path in REQUIRED_FILES),
    },
    {
        "name": "both_domains_present",
        "passed": {"math", "code"} <= {trace["domain"] for trace in traces},
    },
    {
        "name": "trace_types_present",
        "passed": {"positive", "negative", "repair"} <= {trace["trace_type"] for trace in traces},
    },
    {
        "name": "train_val_no_overlap",
        "passed": not ({record["record_id"] for record in train} & {record["record_id"] for record in val}),
    },
]
```

These inspection items implement "project inspections" into operational engineering constraints.

In the process of P06, in addition to `evaluate_prm.py`, there is also `run_p6_checks.py`. This shows that the project not only focuses on whether data can be generated, but also whether the code, products, and reports are consistent.

### 15.1 Why the evaluation cannot only look at the model performance after training

What the project chapter needs to show most is the "engineering closed loop" rather than the final model score. Because in many real teams, the real first problem is not the PRM model itself, but:

* A certain key product is leaked;
* train/val overlap;
* Certain types of step labels are not covered at all;
* reward bucket is missing;
*The code and report are not written in the same data.

### 15.2 What does the check coverage of the current project indicate?

Current inspection results show:

* General inspection items: `10`
* Passed check item: `10`
* Overall status: `PASS`
* Command level check coverage: `py_compile, evaluate_prm`
* Data level check coverage: `required_files_exist, both_domains_present, trace_types_present, step_labels_cover_both_classes, reward_buckets_present, train_val_no_overlap ...`

This shows that the inspection of the current project is not just a formality, but actually covers key aspects such as code runnability, file existence, data distribution and training segmentation.

### 15.3 Why check scripts deserve to be included in the main body of the chapter

A PRM project without an inspection layer can easily look normal during the demonstration phase and completely out of control during the replication phase. Writing the inspection script into the main body of the chapter essentially emphasizes:

> The credibility of a process supervision project comes not only from good-looking samples, but also from whether it can self-certify that there are no obvious engineering errors.

---

## 16. Noise control: negative and repair trajectory management

P06 What is most worthy of in-depth discussion at present is not how beautifully the positive example trajectory is done, but why the negative example and repair trajectory have become the main shortcomings.

### 16.1 Why are negative examples naturally dirtier?

The problem with negative trajectories is that they tend to be a mixture of two completely different ingredients:

* "Real wrong path" with research value;
* "Random writing noise" with no supervisory value.

If the project does not distinguish between the two, PRM is likely to learn "how to punish strangely styled texts" rather than "how to identify truly faulty reasoning steps."

### 16.2 Why repair trajectories are more prone to ambiguity

The repair trajectory looks ideal because it simulates the process of repairing the model after it makes a mistake. But what’s really difficult is:

* Which step should the repair start from?
* Whether to retain the previous error steps;
* Whether the correct steps after repair require downgrading;
* Fixed whether the trajectory will cause stylistic deviation.

If these issues are not clearly written in the schema and validation layer, repair data will soon become the most unstable type of asset.

### 16.3 Clear signals given by the current project

Existing reports have clearly pointed out that the shortcomings in the overall pass rate are mainly concentrated in the negative and repair traces. Subsequent optimization should give priority to trace verification and repair trace quality control, rather than blindly expanding the scale.

This is a very important engineering conclusion because it converges "what to do next" from a general discussion into a clear production line problem.

![Figure 10: negative / repair noise source map](../../images/part10/10_6_fig10_noise_sources.png)

---

## 17. Costs and Benefits: Prioritization of Structural Closure

In many teams, when it comes to process supervision, they immediately think of longer CoT, more complex annotations, and higher training costs. This judgment is not completely wrong, but it is also easily misleading:

What is really expensive is never "a few more steps", but blind expansion without verification of the closed loop.

### 17.1 Real benefits of small-scale projects

The greatest value of a small-scale process monitoring project like P06 is not to immediately produce a strong PRM, but to first address the following issues:

* Whether step can be cut;
* Whether the label is stable;
* Whether reward can be explained;
* Whether train/val is controllable;
* Whether the noise can be located.

Once these basic problems are not solved, the larger the scale, the faster the dirty data will expand.

### 17.2 Why this chapter places more emphasis on “structural benefits”

P06 is currently not large in scale, but it already has the following key features:

* Has a complete stage link;
* There are real indicators;
* There are clear shortcomings;
* There are training interface products;
* There is a closed loop of inspection and evaluation.

This means that its main value lies in "explaining process supervision clearly in an engineering manner" rather than "using massive data to prove that PRM must be effective."

### 17.3 A more realistic engineering judgment

For most teams, the first version of the PRM project is more suitable to follow this principle:

> First make process signals into credible assets, and then discuss how to amplify the assets.

---

## 18. Main deliverables: product list

The project chapter should not only describe "what was done", but also "what was left in the end".

Judging from the existing reports, P06 has formed a relatively complete deliverable system:

### 18.1 Intermediate data products

* `data/processed/seed_pool.jsonl`
* `data/processed/task_spec.json`
* `data/processed/cot_traces.jsonl`
* `data/processed/trace_summary.json`
* `data/processed/validated_traces.jsonl`
* `data/processed/step_rewards.jsonl`
* `data/processed/validation_summary.json`

### 18.2 Training data products

* `data/training/prm_step_dataset.jsonl`
* `data/training/train.jsonl`
* `data/training/val.jsonl`
* `data/training/smoke_test.jsonl`
* `data/training/training_manifest.json`

### 18.3 Reporting and Inspection Products

* `data/reports/p6_report.md`
* `data/reports/p6_metrics.json`
* `data/reports/p6_test_results.json`
* `data/reports/p6_test_report.md`

Such a deliverable structure shows that the project is no longer just a demonstration in a notebook, but a relatively complete set of data engineering outputs.

---

## 19. Limitations and Risks: Current Process Oversight Constraints

The most common mistake that many engineering cases make is to only talk about their own structure and results, but not their own vulnerabilities.

But for PRM projects, limitations cannot be omitted. Instead, they should be placed in a later main chapter and clearly written out.

### 19.1 Main limitations of the current project

According to existing reports, the main limitations of P06 include at least:

* The overall verification pass rate is still only `67.59%`;
* Negative and repair trajectories are more likely to introduce noise and label ambiguity;
* Currently only covers two types of reasoning tasks: mathematics and code;
* The data size is not suitable to be directly used as a large-scale PRM training database.

Rather than diminishing the value of the project, these limitations make the chapter more believable because they illustrate that the project does not avoid problems but exposes them.

### 19.2 Why “expanding volume first” may be the wrong direction

Existing reports clearly indicate that if we continue to scale up without first improving cleaning and validation, PRM data will become dirty rather than stronger. This judgment is very critical because it brings the project priorities back to the correct order:

* Improve trace quality first;
* Improve reward credibility;
* Finally, expand task coverage and data scale.

### 19.3 Why these risk judgments must be retained

Because the cases that will really be reused by the team are usually not cases that "claim that it has been done", but cases that "explain clearly what should be done next".

---

## 20. Subsequent expansion: Toward a more complex process monitoring system

P06 has currently set up a good starting point, but its greater value lies in retaining a clear path for subsequent expansion.

### 20.1 Expansion direction one: Expand the scope of tasks

Existing suggestions have pointed out that the scope of the task can be further expanded to:

* Table reasoning
* Scientific reasoning
* Planning reasoning

The common feature of this type of tasks is that the process is more complex and verification is more difficult, which therefore better reflects the necessity of step-level supervision.

### 20.2 Expansion direction two: refine the definition of reward

Currently, the reward bucket already has a basic level, but it can be further refined in the future:

* Distinguish between local correctness and global correctness;
* Distinguish between formal errors and logical errors;
* Distinguish between fixable errors and unfixable errors.

This makes the process signal more suitable for downstream PRM or hybrid training paradigms.

### 20.3 Expansion direction three: Migrate to complex Agent tasks

From a methodological point of view, P06 currently has migration potential. Because many Agent tasks are inherently multi-step processes:

*Plan first;
* Execute again;
* Observe again;
*Modify again.

As long as the definition of "step" is expanded from text reasoning to actions and states, the PRM data factory method has the opportunity to migrate to more complex scenarios.

---

## 21. Summary: The value of trusted process signals

What is most worth retaining about P06 is not the large-scale PRM data set it has produced, but that it clearly demonstrates a very critical engineering judgment:

> Result supervision focuses on whether the model got the answer correct in the end, while process supervision focuses on how the model arrived at this result.

Judging from the existing project materials, P06 already has several key engineering features:

* Have clear task boundaries;
* There is a structural link from seed to trace to step;
* There is an explicit distinction between three types of trajectories;
* There is automatic verification based on rules, execution and result comparison;
* There are reward bucket and process-only signal;
* There are training interface products and inspection closed loop;
* There are also clear noise shortcomings and follow-up directions.

This means that it is no longer just a "data demo with CoT", but closer to a process supervision engineering case for the team to refer to.

It can be summed up in one sentence:

> What the PRM data factory really wants to build is not a longer answer, but a more trustworthy process.

---

## Special topic: Annotation consistency and QA mechanism of PRM data

The most underestimated part of a process monitoring project is not the model invocation itself, but the annotation consistency. Because result supervision only needs to judge "whether the final step is correct", while process supervision needs to judge "whether each step in the middle is on the right track." This significantly increases the complexity of annotation and QA.

### 1. Why are step-level annotations more prone to ambiguity?

For the same question, it is usually easier for two reviewers to agree on the final answer, but they are likely to give different judgments on the quality of the intermediate steps. For example, one-step derivation may result in correct results, but skip key explanations; or the local expression may not be rigorous enough, but the overall direction does not deviate. Similar situations have less impact in outcome supervision, but will directly change the reward signal in PRM.

Therefore, QA for projects such as P06 cannot only focus on “are there any errors?”, but must also focus on:

* Whether this step can be accepted as a training signal;
* The error in this step is a formal error, a logical error, or a repairable error;
* Whether this step still retains valuable information for subsequent steps;
* Whether this track should enter the repair channel instead of discarding it directly.

Only by writing these questions clearly can the team advance step-level supervision from "subjective impression" to "structured judgment."

### 2. Process supervision requires hierarchical QA

Compared with traditional SFT, PRM is more suitable for adopting hierarchical QA mechanism. A more practical way to divide it is:

* Sample layer QA: Check whether the task, answer and trajectory match;
* Step layer QA: Check the format, logical continuity and local correctness of each step;
* Trajectory layer QA: Check whether the three types of trajectories, success, negative, and repair, are established respectively;
* Dataset layer QA: Check whether reward bucket, task distribution and train/val split are stable.

The value of this layered approach is that it helps the team quickly locate the source of noise. Otherwise, once it is discovered that the training effect is not good, all problems will be crudely attributed to "the PRM data is not good enough", but no one knows whether it is a problem with step segmentation, label bucketing, repair design, or trajectory distribution.

### 3. QA not only deletes errors, but also retains “useful errors”

Another important difference between PRM data and ordinary high-quality question and answer data is that it should not treat all faulty processes as worthless. Many repair trajectories rely on the structure of "make mistakes first, then correct" to form process signals that are valuable to the model.

Therefore, the goal of QA should not just clear errors, but determine:

* Which errors contaminate supervision and should be eliminated;
* Which errors are incorrect but have clear repair paths and are suitable to be retained as repair assets;
* Which errors can be converted into failure replay for subsequent rule enhancement;
* Which errors indicate that the step segmentation method itself needs to be adjusted.

Once the team has established this mindset, it will no longer use the idea of ​​​​"pure data sets" to accidentally damage some of the most valuable signals of PRM.

---

## Special topic: PRM strategy selection when entering the training system

P06 has made the process supervision data into a training interface, but when actually entering training, you will encounter another practical problem: how to use these process signals. Different teams and different stages often use PRM data in very different ways.

### 1. PRM data does not necessarily need to be trained separately as soon as it comes up.

In many real projects, PRM data does not independently undertake all training tasks. A more common approach is to combine it with outcome supervision data, SFT data or preference data to form a combined training strategy. The reason is that process signals are good at shaping reasoning paths, but may not necessarily cover style, rejection, safety, and task breadth alone.

Therefore, a more realistic training perspective is usually:

* Use result supervision to ensure answer boundaries;
* Use process supervision to enhance the quality of intermediate reasoning;
* Use preference or rule data to constrain output behavior;
* Use smoke sets and replay sets to continuously observe regression.

From a systems engineering perspective, PRM does not replace everything, but supplements the ability of "why the model reasons like this".

### 2. Different stages are suitable for different usage methods

Early in a project, PRM data is better suited as a “structural verification asset.” In other words, use it first to verify whether the step segmentation, reward logic and training template are reliable, rather than pursuing large-scale gains from the beginning. As the data quality improves, it will gradually be placed in a more important position.

This usually means:

* In the first stage, we first look at the data structure and training readability;
* The second stage is to see whether the process improvement on a small number of tasks is clear;
* In the third stage, we will consider expanding task coverage and training proportion;
* Only in the fourth stage will we discuss whether to form a stable PRM training subsystem.

The value of this incremental approach is to prevent the team from overcommitting when the process signals are not yet stable.

### 3. The PRM project will eventually form its own version language

As P06 continues to iterate, the team will increasingly need a language dedicated to describing versions of PRM data. For example:

* What tasks have been expanded in this version;
* step whether the segmentation rules change;
* Whether reward bucket is redefined;
* Whether the proportion of repair tracks increases;
* Which high-noise samples are removed;
* Which replay issues have been absorbed.

Only when this information is stably retained can the team truly evaluate “how is this version of PRM better than the last version?” Otherwise, even if the training results change, it is difficult to determine which process signal adjustment the change comes from. For a highly structured data type like process supervision, the version language itself is part of the engineering capability.

---

## Special topic: The replay set value of process supervision projects

P06 This kind of project also has another asset that is worth retaining for a long time, which is the replay set. Because many problems in process supervision will not appear immediately in the overall indicators, but will appear repeatedly on certain typical trajectories. As long as these high-frequency problems are precipitated into replays, the team can judge more quickly in each iteration: whether the current changes are improving the real problems or just making the indicators look smoother.

### 1. Which questions are most suitable for collecting in the replay set?

For PRM, the most worthy replay sets are usually not random failures, but problems that consistently undermine the credibility of process signals, such as:

* step segmentation leads to context break;
* The repair track looks like repair, but actually it just rewrites the answer;
* Some steps are reasonable, but the overall direction has already deviated;
* The reward bucket gives unstable allocations for similar errors.

Once these problems are fixed, they can become important regression samples for subsequent rule changes, verification changes, and training template changes.

### 2. Replay sets can help the team build "problem memory"

What many process supervision projects fear most is not the current noise, but the repeated exposure to the same noise every time. The role of the replay set is to turn these problems from one-time experiences into project memories for continuous review. As long as that memory remains, it will be easier for the team to answer a key question: Is this version of P06 really better, or is it just a repeat of the same problem in a different way?

